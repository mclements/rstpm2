.setUp <- function() {
  suppressMessages( require(rstpm2) )
  data(brcancer)
  fit <- stpm2(Surv(rectime,censrec==1)~hormon,data=brcancer,df=5)
}

test.basic.1 <- function() {
  checkEqualsNumeric(round(coef(fit)[2],5), -0.36457)
}



## .tearDown <- function() {
##   rm(fit)
## }
